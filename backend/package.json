{
  "name": "fanoron-telo-backend",
  "version": "1.0.0",
  "description": "Backend (moteur de jeu + IA) du Fanoron-telo",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "benchmark": "node scripts/benchmark.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
